# Enforce LF for shell scripts so they run correctly on Windows (Git Bash)
*.sh text eol=lf

# Enforce LF for text files so CI-generated content matches committed content
*.md   text eol=lf
*.json text eol=lf
*.yaml text eol=lf
*.yml  text eol=lf