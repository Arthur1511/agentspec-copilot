{
  "version": 1,
  "hooks": {
    "sessionStart": [
      {
        "type": "command",
        "bash": "{{plugin_data_dir}}/scripts/init-workspace.sh",
        "powershell": "{{plugin_data_dir}}/scripts/init-workspace.ps1",
        "timeoutSec": 30
      }
    ]
  }
}
